# Sinergy — Handoff (2026-08-18)

**Статус:** временно приостановлено (пауза по запросу пользователя). Вернуться позже.

## Objective

Исправить: (1) краткий summary на карточках новостей, (2) блендер не работал.
Оба исправлены, кроме дособора обогащения уже собранных новостей.

## Что сделано (закоммичено + запушено в main)

- **Блендер работает:** `POST /api/sinergy/find-next` возвращает `synergy_found`
  (CorridorOS, FleetMesh, AegisSky Sentinel и др.). Default mode `det-only`
  (избегает 60s+ Vercel timeout от Optimist/Skeptic).
- **saveSynergy исправлен:** убран несуществующий столбец `source_type`
  (падал insert синергии-идеи → `idea_id: null`). Теперь синергия сохраняется как карточка.
- **Каскад LLM из двух провайдеров:** VPS OmniRoute (OpenCode Zen, free) primary +
  OpenRouter free fallback (через наш `OPENROUTER_API_KEY`). `OMNIROUTE_ENABLED`
  включается при `!== 'false'`.
- **Авто-обогащение в cron исправлено:** `enrichRawItems` (discovery.ts) теперь
  извлекает `core_tech`/`vertical`/`target_audience` (раньше только перевод →
  новые новости оставались без `core_tech` → краткий summary).
- **LLM ускорен:** `max_tokens` 8192→2048, `auto/fast` первым, `VPS_TIMEOUT_MS` 15s→4s.

## Что осталось (НЕ доделано)

- **~191 новость без `core_tech`** (собраны после ручного прогона, помечены
  `is_extracted=true` старым переводом → следующий cron их пропустит).
  Нужен дособор через `enrich-raw` (endpoint, auth временно отключен).
  Было запущено, остановлено по `pkill` (прогнал ~42 из 191).
- **VPS OpenCode мёртв:** у провайдера исчерпаны OpenRouter-credits
  (`Insufficient credits`). Каскад уходит на наш OpenRouter (работает).
  Когда провайдер пополнит credits → VPS оживёт без код-изменений.
- **`enrich-raw` auth отключен** (temp, commit `7a10b5e`) — вернуть Bearer-проверку
  `CRON_SECRET` после использования.

## Файлы

- `src/lib/ai/omni.ts` — VPS primary + OpenRouter fallback, auto/fast, timeout 4s
- `src/lib/ai/openrouter.ts` — OR_MODELS (nemotron free и др.)
- `src/lib/sinergy/agents/orchestrator.ts` — selectPairs (core_tech filter), runBlender (break после 1), saveSynergy (без source_type)
- `src/app/api/sinergy/find-next/route.ts` — default `det-only`
- `src/app/api/sinergy/admin/enrich-raw/route.ts` — параллель ×3, auth отключен
- `src/lib/sinergy/discovery.ts` — enrichRawItems заполняет core_tech

## Next steps (при возврате)

1. Дособрать обогащение: `curl -X POST .../enrich-raw?limit=10` в цикле до `remaining=0`.
2. Вернуть auth в `enrich-raw` (`CRON_SECRET`).
3. (Опц.) Пополнить VPS-credits провайдеру → VPS ветка оживёт.
4. Проверить, что feed-карточки показывают полный summary (description 374–566 знаков).

## Escalation: false
